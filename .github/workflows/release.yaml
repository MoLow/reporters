name: release

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      packages:
        description: 'JSON array of package names to publish (e.g. ["@reporters/bail","@reporters/github"]); leave empty to only deploy the viewer'
        required: false
        default: ''
        type: string

jobs:
  release-please:
    if: ${{ github.event_name == 'push' }}
    runs-on: ubuntu-latest
    outputs:
      releases_created:  ${{ steps.release.outputs.releases_created }}
      packages:  ${{ steps.get-released-packages.outputs.result }}
    steps:
      - name: Create GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.PR_CREATOR_APP_ID }}
          private-key: ${{ secrets.PR_CREATOR_PRIVATE_KEY }}
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          token: ${{ steps.app-token.outputs.token }}
      - uses: actions/checkout@v5
        if: ${{ steps.release.outputs.releases_created }}
      - id: get-released-packages
        if: ${{ steps.release.outputs.releases_created }}
        uses: actions/github-script@v8
        with:
            github-token: ${{ steps.app-token.outputs.token }}
            script: |
              const path = require('path');
              const outputs = ${{ toJSON(steps.release.outputs) }};
              return Object.keys(outputs)
                .map((key) => {
                  const match = key.match(/^(.*\/.*)--release_created$/);
                  if (!match) return;
                  return require(path.join('${{ github.workspace }}', match[1], 'package.json')).name;
                })
                .filter(Boolean);

  npm-publish:
    needs: release-please
    if: ${{ always() && ((github.event_name == 'workflow_dispatch' && inputs.packages != '') || (needs.release-please.result == 'success' && needs.release-please.outputs.releases_created && needs.release-please.outputs.packages && fromJson(needs.release-please.outputs.packages || '[]')[0] != null)) }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        package: ${{ fromJson(github.event_name == 'workflow_dispatch' && inputs.packages || needs.release-please.outputs.packages || '[]') }}
    steps:
      - uses: actions/checkout@v5
      - run: corepack enable
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          registry-url: 'https://registry.npmjs.org'
      - run: yarn install --immutable
      - id: package-name
        run: |
          PACKAGE_NAME=$(node -p "PACKAGE_NAME='${{ matrix.package }}'.split('/')[1]")
          echo "PACKAGE_NAME=$PACKAGE_NAME" >> $GITHUB_OUTPUT
      - run: yarn npm publish --provenance --access public
        working-directory: packages/${{ steps.package-name.outputs.PACKAGE_NAME }}

  deploy-viewer:
    needs: release-please
    if: ${{ always() && ((github.event_name == 'workflow_dispatch' && inputs.packages == '') || (needs.release-please.result == 'success' && contains(fromJson(needs.release-please.outputs.packages || '[]'), '@reporters/web'))) }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    concurrency:
      group: pages
      cancel-in-progress: true
    environment:
      name: github-pages
      url: ${{ steps.deploy-retry.outputs.page_url || steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v5
      - run: corepack enable
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
      - run: yarn install --immutable
      - run: yarn workspace @reporters/web build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: packages/web/dist/viewer
      - id: deploy
        uses: actions/deploy-pages@v4
        continue-on-error: true
      - id: deploy-retry
        if: ${{ steps.deploy.outcome == 'failure' }}
        uses: actions/deploy-pages@v4